configfile: "config.yaml"


include: "rules/common.smk"
include: "rules/adapters.smk"


rule all:
    input:
        expand(
            "reads/adapter_trimmed/{pool}.fastq",
            pool=get_pool(),
        ),
